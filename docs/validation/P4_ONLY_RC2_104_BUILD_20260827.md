# P4-only RC2-104 software validation — 2026-08-27

## Scope

This record covers the uncommitted P4-only source transition on branch
`feat/p4-dual-usb-host`, based on commit
`6e41d7f9f5634b5dd36ca664aea50be265fe12c3`. The application reports
`RC2-104-g6e41d7f-dirty` because the validated refactor was still in the working
tree. Nothing was flashed or hardware-accepted.

The active product now has one firmware target. P4 directly owns USB0 storage,
USB1 controller MIDI/audio, semantic-event mapping, profiles and LED output.
S3 UART/heartbeat/debug AP, profile transfer, monitor PCM, firmware reporting,
CI build and OTA packaging are absent from the active path.

## Verification

- ESP-IDF environment: `ESP-IDF v6.0.2`.
- `tests/run_p4_host_tests.ps1`: passed, including P4-only CMake/package guards
  and 393 audio-engine assertions.
- clean `build_p4_only`: passed after `fullclean` and `set-target esp32p4`.
- configure component list contains `control_link`, `controller_runtime`,
  `controller_usb_host`, `controller_usb_audio` and `p4_local_controller`, but
  no `monitor_pcm_link` component.
- `control_link` compiled only `control_link_local.c` and
  `flx4_led_snapshot.c`.
- ELF symbol audit found no `control_link_uart`, `ctrl_bulk`, `cp_xfer`,
  `monitor_pcm`, S3 firmware-report or legacy control-state sender symbol.
- UI simulator navigation passed; the visually reviewed Settings baseline now
  shows `Controller (USB1)` and no S3 control/status rows.
- P4-only OTA packaging and independent bundle/manifest signature verification
  passed with key ID `rel-001`.

Build command:

```powershell
. C:\Espressif\tools\Microsoft.v6.0.2.PowerShell_profile.ps1
Set-Location firmware\main-deck-p4
idf.py -B build_p4_only fullclean
idf.py -B build_p4_only -D SDKCONFIG=build_p4_only/sdkconfig set-target esp32p4
idf.py -B build_p4_only -D SDKCONFIG=build_p4_only/sdkconfig build
```

## Application image

| Bytes | OTA slot | Slot free | P4-only budget free | SHA-256 |
| ---: | ---: | ---: | ---: | --- |
| 2,445,904 | 4,194,304 | 1,748,400 | 1,224,112 | `152ca38af87cba70d5b54369994a07ddb789c4a40f1364236c6e47b22634b212` |

## Signed P4-only package

Ignored output directory:
`releases/pajoniiir-RC2-104-g6e41d7f-dirty/`.

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `main-deck-p4.bin` | 2,445,904 | `152ca38af87cba70d5b54369994a07ddb789c4a40f1364236c6e47b22634b212` |
| `main-deck-p4.ddjota` | 2,446,092 | `29d073fb017dd3d4712c850f6cfd50369f7958c5f49f9600c370e5c33c3e0d4d` |
| `manifest.json` | 662 | `0eedd98e49647584df503a392ed8c78241c5cc2b931ddea76fc94430bccef959` |
| `manifest.sig` | 64 | `f0fb714031996d9234e61d8373175f6221210b7ed48ef2b4ab6ebbc6b97e3c88` |

The manifest contains exactly one target: `p4`, project `main-deck-p4`, chip ID
`0x0012`. No S3 binary or bundle was emitted.

## Open boundary

This is software evidence only. Protected VBUS, stable common 5 V power,
dual-root boot/insertion order, complete direct MIDI/LED/UAC behavior,
independent recovery, brownout-free combined load and the 30-minute soak remain
open before merge or release qualification.
