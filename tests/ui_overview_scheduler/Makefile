# Makefile for Overview redraw scheduler helper tests

CC      = gcc
CFLAGS  = -Wall -Wextra -Wpedantic -std=c99 \
           -I../../firmware/main-deck-p4/components/ui/include

SRCS    = test_ui_overview_scheduler.c \
          ../../firmware/main-deck-p4/components/ui/ui_overview_scheduler.c

TARGET  = test_ui_overview_scheduler

.PHONY: all test clean

all: $(TARGET)

$(TARGET): $(SRCS)
	$(CC) $(CFLAGS) -o $@ $^

test: $(TARGET)
	./$(TARGET)

clean:
	rm -f $(TARGET)
