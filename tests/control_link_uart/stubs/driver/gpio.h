#pragma once

/* control_link_uart.c only names GPIO numbers for uart_set_pin(); nothing is
 * driven, so the pin identities just have to exist and be distinct. */

typedef int gpio_num_t;

#define GPIO_NUM_28 28
#define GPIO_NUM_29 29
